# O site funciona SEM nenhuma variável de ambiente: por padrão usa o projeto
# Supabase atual do Dr. Santiago (valores em src/lib/supabase.ts).
#
# Só descomente as duas linhas abaixo se for apontar para OUTRO projeto Supabase,
# e preencha as duas — variável declarada vazia é ignorada de propósito.

# VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
# VITE_SUPABASE_ANON_KEY=sua-anon-key
